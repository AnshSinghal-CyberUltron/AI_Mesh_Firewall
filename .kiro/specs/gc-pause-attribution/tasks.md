# Task 7 — tasks

- [ ] 7.1 `_GCMonitor` on `gc.callbacks`, env-gated, O(1), cannot raise
- [ ] 7.2 Per-request delta captured at request start, surfaced as `gc_pause_ms`
- [ ] 7.3 Unit tests: counters move on a forced collection; nothing registered when off
- [ ] 7.4 Full suite — zero new failures
- [ ] 7.5 E2E `--repeat 2`; read gc_pause_ms at median vs tail; record either outcome
