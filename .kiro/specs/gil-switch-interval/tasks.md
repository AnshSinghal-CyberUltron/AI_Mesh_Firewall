# Tasks

- [ ] **T1** Env-gated `sys.setswitchinterval()` at startup; default unchanged.
- [ ] **T2** Verify the running container reports the intended interval before measuring.
- [ ] **T3** Four-arm A/B, `--repeat 3`, stub pinned; report p50/p90/p99 with spread, RPS, CPU.
- [ ] **T4** Record the result — explicitly including refutation if p99 does not move.
