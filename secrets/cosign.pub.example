# Placeholder — replace with a real Cosign public key for MODULE3_ADMISSION_MODE=verify.
#
# Generate a pair (writes secrets/cosign.key + secrets/cosign.pub):
#   scripts/module3_gen_cosign_keypair.sh
#
# Or:
#   cosign generate-key-pair
#   cp cosign.pub secrets/cosign.pub
#
# Then:
#   docker compose -f docker-compose.yml -f docker-compose.module3-verify.yml up -d gateway
#
# This file is NOT a valid Cosign key. Do not point MODULE3_COSIGN_PUBLIC_KEY_FILE
# at cosign.pub.example in production.
