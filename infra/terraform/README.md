# Terraform (placeholder)

Planned modules:

- `network` — VPC, subnets, WAF, ALB
- `data_plane` — gateway ASG, guardrails ASG
- `control_plane` — control API ASG
- `workers` — Celery worker ASG (queue-tagged)
- `data` — Aurora, ElastiCache, Amazon MQ

Initialize after Phase 1 code extraction is complete.
