
__pycache__/
*.pyc
.venv/
node_modules/
dist/
*.egg-info/
.pytest_cache/
.coverage
postgres_data/
*.log
.DS_Store

# Verification run artifacts (may contain bearer tokens, payloads, screenshots)
runs/verification/

# EC2 static UI (synced from frontend/dist — not committed)
deploy/static/

# Terraform cache
**/.terraform/*

# State files
*.tfstate
*.tfstate.*

# Crash logs
crash.log

# Keep dependency lock file
!.terraform.lock.hcl

# Personal scratch file at repo root — must never be committed (file stays local)
/temp.py.env

# Local env with secrets (never commit)
.env
.env.bak
.playwright-mcp/
/tmp/
.skill-workspace/
