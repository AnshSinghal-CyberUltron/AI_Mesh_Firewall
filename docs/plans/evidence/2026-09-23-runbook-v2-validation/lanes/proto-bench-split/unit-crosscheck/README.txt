The unit lane's p22u runs (rv-proto-unit-1, g2-standard-24 all-in-one, 18 workers + 2 local owners, same
admission config AMF_TARGET_P99_MS=1000 RV_GUARD_DEADLINE_MS=20, HEADLINE 22M, org-a) recomputed READ-ONLY
from their raw files (/home/contact_cyberultron_com/rv-evidence-raw/proto-bench-unit/runs/<run>) with THIS
lane's tools/split_metrics.py, i.e. identical corrected rules (cohort = offered - policy blocks, infra = +inf;
SSE-first = event completing provider token 1, on their 10% per-chunk sample). Not the unit lane's own verdicts.
