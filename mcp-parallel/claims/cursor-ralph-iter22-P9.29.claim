agent: cursor-ralph-iter22
story: P9.29 (item 29 — sustained load, CURSOR angle: broker sandbox reuse/pooling,
       pids cgroup limits respected, no orphan storm, no 503 storm)
files:
  - scripts/mcp_multi_org_harness.py       # add SUSTAINED load phase (Cursor-owned)
  - mcp-parallel/findings/p9-29/CURSOR_*   # Cursor-distinct filenames (Claude owns other p9-29 files)
  - docs/mcp/PARALLEL_CLAIMS.md
  - .cursor/ralph/scratchpad.md
  - scripts/ralph/mcp_progress.md          # shared with claude-mcp-loop; stage narrowly, rebase
note: |
  Complementary to claude-mcp-item29-P9.29 (scripts/mcp_load_live.py = gateway/control
  reliability ceiling). Cursor proves the broker/docker_manager pooling invariants:
  one sandbox per org reused under sustained load (no duplicate containers), cgroup
  pids.current < pids_limit throughout, no orphan sandboxes spawned, no broker 503 storm,
  recovery after load. Does NOT touch scripts/mcp_load_live.py or gateway/broker routes.
since: 2026-07-02T10:22:00Z
status: active
