# Phase-3 observability LIVE verification (backstop CHG-0020, 2026-07-02)

METRICS (WIRED):
  gateway /metrics -> 401 (exists; scraper-key-gated; METRICS_ALLOW_OPEN=false -> secured, not open)
  gateway env: METRICS_SCRAPER_KEY set; TELEMETRY_DRAIN_MODE=thread; TELEMETRY_DRAIN_INTERVAL_SEC/BATCH_SIZE set
  (telemetry drain thread active; GATEWAY_MONGO_TELEMETRY_ENABLED=false)

HEALTH (WIRED):
  gateway /health -> 200 ; gateway /v1/mcp/health -> 200
  control /api/health/ -> 200
  broker(8311) /health -> 200

AUTO-RECOVERY (partial):
  docker healthchecks: broker=healthy, control=healthy, postgres=healthy, redis=healthy (auto-restart on unhealthy)
  sandbox reaper/reconcile (code-verified) -> sandbox self-heal
  GAP: gateway container has NO docker healthcheck (health=none) -> docker won't auto-restart it on failure

GAPS (item 13):
  - Distributed TRACING (OpenTelemetry/Jaeger): NOT configured (no OTEL/JAEGER/TRACING env on gateway/broker).
  - Gateway lacks a docker healthcheck.
  - Backup (PG/Redis): NOT verified (no backup cron/volume probed).
