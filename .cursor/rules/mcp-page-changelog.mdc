---
description: MCP page (firewall-1-4) cleanup changelog — clean errors, stuck-state, redactions, banner, alignment
globs: frontend/src/components/MCPConnectorPanel.jsx, frontend/src/components/MCPScanControlMatrix.jsx, gateway/ai_mesh_gateway/mcp_proxy.py, gateway/ai_mesh_gateway/mcp_stdio_adapter.py
alwaysApply: false
---

# MCP Page Cleanup Changelog (program claude-mcp-page-cleanup)

One item/iteration; never fake green; four-memory log every change (id|files|WHAT|WHY|NOW DOES|AFFECTS|VERIFY).

- MCP-PAGE-CLEANUP-00 (page ownership) | claims/claude-mcp-page-cleanup-C0.claim; MCP_PAGE_CHANGELOG.md; this file; AGENTS.md | CLAIM the MCP page (firewall-1-4) frontend (MCPConnectorPanel/MCPScanControlMatrix/PolicyManagementPanel + 1.4 slices) + MCP error/state backend (mcp_proxy discovery+transport errors, mcp_stdio_adapter stdio-start, mcp_connector/views sync classifier). Cursor's broad frontend/** + chat-pipeline carve-out respected. | VERIFY: claim file + ledger row; branch main.

- MCP-PAGE-CLEANUP-01 (gateway classifier) | gateway/ai_mesh_gateway/mcp_error_classifier.py + test | classify_mcp_failure(exc/status/exit_code/raw)->(code,message); sanitize_mcp_error->{error,code,ref}. Covers OOM(-9/137)/CRASH(-6/134/-11/139)/START/AUTH(401/403)/UPSTREAM_HTTP_ERROR(405 echoes number only)/TIMEOUT/DNS/CONN_REFUSED/EGRESS/STORAGE/IMAGE/UNAVAILABLE. Codes mirror control D-codes + DNS/CONN_REFUSED/UPSTREAM_HTTP additions. Raw cause → dev-only log+redis mcp:diag:<ref> (never client). | VERIFY: pytest test_mcp_error_classifier.py 33 passed. Items 02-04 route leak sites through it.

- MCP-PAGE-CLEANUP-03 (stdio-start clean errors) | gateway/ai_mesh_gateway/mcp_stdio_adapter.py + test_stdio_failure_message.py | extracted _stdio_failure_message() called from _start_reader finally; routes the death cause through sanitize_mcp_error. Old safe_msg leaked exit code {rc} + env-var names (MCP_SANDBOX_MEMORY_MB) + '{proc.key}' + 'gateway logs'. NOW: exit-code/env-var/server-key-free; OOM(-9/137/heap)→'exceeded its memory limit', crash(-6/-11/134/139)→'crashed while starting'; raw stays in logs+diag keyed by ref. | VERIFY: pytest test_stdio_failure_message.py 11 passed; full suite 1770 passed. NOTE: item 02 (mcp_proxy discovery) DEFERRED — file has uncommitted changes from another session.

- MCP-PAGE-CLEANUP-02 (discovery clean errors) | gateway/ai_mesh_gateway/mcp_proxy.py + test_mcp_discovery_clean_errors.py | internal_discover_tools: outer except was `Upstream discovery failed: {exc}` (raw exc/HTML) → sanitize_mcp_error(exc)+_discovery_error_response; non-SSE else checks tools_resp.status_code>=400 → "HTTP 405 — check the endpoint URL" + guards .json(). Raw → log+diag by ref. | VERIFY: pytest test_mcp_discovery_clean_errors.py 3 passed (no <html>/nginx/host/Errno leak); full suite 1773 passed. Done after CHG-0120 landed (mcp_proxy.py clean).

- MCP-PAGE-CLEANUP-04 (transport + 401 clean) | gateway/ai_mesh_gateway/mcp_proxy.py + test_mcp_ext_transport_clean_errors.py | ext_mcp_proxy except (was `DNS resolution failed for '{hostname}'`+detail=str(exc) / `unreachable`+detail=str(exc)) + control-proxy except (detail=str(exc)) → sanitize_mcp_error(exc); NEW upstream 401/403 intercept → sanitize_mcp_error(status=401) 're-authenticate'. Raw host/exc/body → log+diag by ref only. | VERIFY: pytest test_mcp_ext_transport_clean_errors.py 3 passed (no host/IP/Errno/token-hint); ext regression 57; full suite 1776. Clean-errors ROUTING now complete at all sites (02/03/04); remaining 05 dev diag endpoint + 06 live verify.

- MCP-PAGE-CLEANUP-05 (dev diagnostic endpoint) | control/.../mcp_connector/views.py + gateway test_mcp_error_classifier.py | control staff-only MCPDiagnosticDetailView now reads gateway diags too via _read_gateway_diagnostic(ref) (raw Redis mcp:diag:<ref>) — django_redis prefixes cache:1:... so cache.get missed the gateway's raw key. Plus always-on structured logs (sanitize logs ref→full cause). | VERIFY: gateway pytest 34 passed (write key mcp:diag:<ref> 7d full-cause, host not in client body); LIVE docker-exec: raw read OK, cache.get None, django key cache:1:mcp:diag:probekey. Control change live on next restart (item 06 deploy).

- MCP-PAGE-CLEANUP-06 (live verify + SSRF-reason fix) | scripts/ralph/mcp_page_cleanup_item06_verify.py; gateway mcp_proxy.py (SSRF, commit deferred) | Deployed 01-05 to gateway + live-verified failing servers show clean errors (cp08 raw-HTML→clean, Linear 401→clean, stdio→clean, all w/ refs). Fixed SSRF-reason leak (3 sites: ext 1841/discover 2552/toolcall 2851 echoed raw errno / 169.254 metadata IP) → sanitize_mcp_error(raw=_reason). | VERIFY: live re-sync clean+refs; full suite 1782 passed. DEFER commit of mcp_proxy.py+tests: parallel session has uncommitted SSE hunk in same file (git add -p blocked). Section A (01-06) complete/verified live; next B (Unknown state 07-09).

- MCP-PAGE-CLEANUP-07 (registration auto-sync) | control/.../mcp_connector/views.py | connection_status only moved off 'unknown' via _resync_server_tools on POST /tools/ (UI calls it; scripts don't → stuck unknown). FIX: registration create sets status='syncing' + _trigger_background_sync() daemon thread runs _resync_server_tools → connected/failed. Never lingers at unknown. Thread owns its DB conn (close_old_connections). | VERIFY: LIVE (control=gunicorn 16 workers, docker cp+HUP) register stdio server w/o /tools/ → 'syncing'→'connected' 13 tools ~2s. Next: item 08 per-server re-sync + resolve existing unknowns.

- MCP-PAGE-CLEANUP-08 (re-sync resolves unknowns) | frontend MCPConnectorPanel.jsx syncServerTools + backend _resync_server_tools (verification only) | per-server re-sync action exists+wired (buttons aria "Sync tools from server" :1396/:1443). LIVE re-synced all 12 stuck unknowns → 6 connected (Everything 1-5 + Filesystem Canary 13/11 tools), 6 failed w/ CLEAN errors (probe/StubBearer unreachable, StubOAuth re-auth, Linear×3 rejected-auth). STILL UNKNOWN: NONE. | VERIFY: bulk re-sync → all left unknown; with item 07 no server stays stuck. Next: item 09 Playwright page verify.

- MCP-PAGE-CLEANUP-09 (Syncing state renders + page verify) | frontend/src/lib/mcpColors.js + item09_verify.mjs | CONNECTION_STATUS lacked "syncing" → item-07 status="syncing" fell through to unknown→rendered "Unknown". FIX: add syncing:{label:"Syncing",warning,pulse} + connecting. | VERIFY: node connectionInfo("syncing")→"Syncing"; build green; LIVE Playwright both themes → Unknown=0, Connected=12/Failed=9, 0 console-err, no overflow. Section B (07-09) complete; next C alignment (10-14).

- MCP-PAGE-CLEANUP-10 (impeccable audit) | docs/mcp/IMPECCABLE_AUDIT_cleanup_1-4.md | audit 1.4 layout 14/16: anti-patterns 4/4 (detect.mjs exit 0), responsive 4/4 (no overflow 4 widths/2 themes, stacks @375), alignment 3/4 (stat-card labels truncate mid-word @1440), spacing 3/4 (minor gap variance). Page already aligned+responsive (CP43/44) → Section C is polish not rebuild. | VERIFY: detect exit 0; Playwright noOverflow; PNGs read. Backlog → items 11-14.

- MCP-PAGE-CLEANUP-11 (stat-card alignment) | frontend/src/components/MCPConnectorPanel.jsx StatCard | truncate→leading-tight (labels wrap, no mid-word clip: "Servers connected"/"244 redact · 0 monitor"); items-center→items-start (values align across cards); Card h-full → equal heights (grid :2361 stretch). | VERIFY: build green; Playwright both themes 0 console-err/no overflow; read PNG labels wrap + equal-height cards.

- MCP-PAGE-CLEANUP-12 (responsive verify + touch) | frontend/src/components/MCPConnectorPanel.jsx + item12_verify.mjs | swept Servers+Observability @1440/1024/768/375 ×2 themes: no overflow, no content bleed (excluded decorative ai-mesh-hero-glow + global nav + inline text links). Fix: 4 icon buttons h-7 w-7(28px)→h-9 w-9(36px). | VERIFY: build green; cleanup12Pass:true (noOverflow/noBleed both tabs, touchOk 24px AA, 0 console-err). Evidence=reflow cards stack @375.

- MCP-PAGE-CLEANUP-13 (impeccable confirm + polish) | frontend/src/components/MCPConnectorPanel.jsx | page already impeccable (detect exit 0, 0 hex, 111 dark:) after CP43/44 + items 10-12 → no destructive revamp. Polish: Top Tools empty-name row was blank+floating count → "(unnamed)" italic + stable key + tabular-nums. | VERIFY: build green; detect exit 0; Playwright regression PASS; PNG shows "(unnamed) 125".

- MCP-PAGE-CLEANUP-14 (snapshot gate + before/after) | scripts/ralph/mcp_page_cleanup_item12_verify.mjs | Section-C gate: sweep Servers+Observability @1440/1024/768/375 ×2 themes → no overflow/bleed, touch≥24px AA, 0 console-err (16 shots). BEFORE cleanup09 (truncated) → AFTER cleanup11/cleanup14 (wraps, equal-height, (unnamed)). | VERIFY: cleanup12Pass:true. Section C (10-14) COMPLETE. Next D redactions=0 (15-16).
