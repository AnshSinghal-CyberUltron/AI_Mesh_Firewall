---
description: MCP page (firewall-1-4) cleanup changelog — clean errors, stuck-state, redactions, banner, alignment
globs: frontend/src/components/MCPConnectorPanel.jsx, frontend/src/components/MCPScanControlMatrix.jsx, gateway/ai_mesh_gateway/mcp_proxy.py, gateway/ai_mesh_gateway/mcp_stdio_adapter.py
alwaysApply: false
---

# MCP Page Cleanup Changelog (program claude-mcp-page-cleanup)

One item/iteration; never fake green; four-memory log every change (id|files|WHAT|WHY|NOW DOES|AFFECTS|VERIFY).

- MCP-PAGE-CLEANUP-00 (page ownership) | claims/claude-mcp-page-cleanup-C0.claim; MCP_PAGE_CHANGELOG.md; this file; AGENTS.md | CLAIM the MCP page (firewall-1-4) frontend (MCPConnectorPanel/MCPScanControlMatrix/PolicyManagementPanel + 1.4 slices) + MCP error/state backend (mcp_proxy discovery+transport errors, mcp_stdio_adapter stdio-start, mcp_connector/views sync classifier). Cursor's broad frontend/** + chat-pipeline carve-out respected. | VERIFY: claim file + ledger row; branch main.

- MCP-PAGE-CLEANUP-01 (gateway classifier) | gateway/ai_mesh_gateway/mcp_error_classifier.py + test | classify_mcp_failure(exc/status/exit_code/raw)->(code,message); sanitize_mcp_error->{error,code,ref}. Covers OOM(-9/137)/CRASH(-6/134/-11/139)/START/AUTH(401/403)/UPSTREAM_HTTP_ERROR(405 echoes number only)/TIMEOUT/DNS/CONN_REFUSED/EGRESS/STORAGE/IMAGE/UNAVAILABLE. Codes mirror control D-codes + DNS/CONN_REFUSED/UPSTREAM_HTTP additions. Raw cause → dev-only log+redis mcp:diag:<ref> (never client). | VERIFY: pytest test_mcp_error_classifier.py 33 passed. Items 02-04 route leak sites through it.

- MCP-PAGE-CLEANUP-03 (stdio-start clean errors) | gateway/ai_mesh_gateway/mcp_stdio_adapter.py + test_stdio_failure_message.py | extracted _stdio_failure_message() called from _start_reader finally; routes the death cause through sanitize_mcp_error. Old safe_msg leaked exit code {rc} + env-var names (MCP_SANDBOX_MEMORY_MB) + '{proc.key}' + 'gateway logs'. NOW: exit-code/env-var/server-key-free; OOM(-9/137/heap)→'exceeded its memory limit', crash(-6/-11/134/139)→'crashed while starting'; raw stays in logs+diag keyed by ref. | VERIFY: pytest test_stdio_failure_message.py 11 passed; full suite 1770 passed. NOTE: item 02 (mcp_proxy discovery) DEFERRED — file has uncommitted changes from another session.
