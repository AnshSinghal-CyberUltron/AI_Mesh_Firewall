# MCP Gateway Ralph Progress   (mark [x] when DONE + verified; never fake green)

## Codebase Patterns (append reusable learnings at top)
- gateway MCP: mcp_proxy / mcp_oauth(_proxy) / mcp_stdio_adapter / mcp_ws_adapter / mcp_sandbox_client
- broker: services/mcp-broker (docker_manager, registry, reaper, docker_health, routes; sandbox-image/agent/stdio_manager)
- control: control/ai_mesh_control/mcp_connector (serializers, mcp_firewall_client, resync command)
- frontend: MCPConnectorPanel(Inner) register/authorize/list/execute; Dialog = createPortal, returns null when closed
- INVARIANT: OAuth requires an HTTP URL; stdio has none → oauth+stdio is invalid, must be blocked in the form
- INVARIANT (leak): one sandbox per org; no cross-tenant tool/data/credential visibility; egress bytes are truth
- B2 FIX (item#15, frontend renderServerCard): B2 was PARTIAL — a fresh HTTP-oauth server showed pending CUES ("authorization required" badge + disabled Sync + Authorize btn) but ALSO a generic grey "Unknown" connection badge + "0 tools", reading like a normal ready card. FIX (MCPConnectorPanel.jsx renderServerCard): compute `awaitingAuth = !srv.needs_reauth && serverAwaitingAuth(srv)`; when true, (1) the PRIMARY connection badge becomes amber "Pending authorization" (replaces the "Unknown"/conn badge, and folds in the old separate "authorization required" badge), (2) the tools line shows amber "Authorize to load tools" instead of "N tools". Sync already gated by syncBlockedForAuth. serverAwaitingAuth covers BOTH auth_type=oauth (!oauth_authorized) AND stdio+mcp-remote (0 tools + not connected) so Linear pending is consistent. Tools-only-after-auth is structurally guaranteed (sync disabled pre-auth + backend creates tool rows only on sync). NOTE: badge text changed "authorization required"→"Pending authorization" — any harness regex must match /pending authorization/i (updated b1 harness). Verified: scripts/playwright_mcp_b2_verify.mjs 6/6 + visual screenshot (amber Pending authorization badge + Authorize to load tools).
- MANUAL-OAUTH HEADED BROWSER (user-mandated; reusable for #16 authorize→populate + #31): host is headless (no DISPLAY) BUT Xvfb present + sudo passwordless + apt ok. Installed x11vnc/novnc/websockify/fluxbox. HARNESS: scripts/ralph/headed_browser.sh {start|status|stop} → Xvfb :99 + fluxbox + x11vnc + noVNC(websockify) on 0.0.0.0:6080 + system chromium (v149) HEADED with CDP on 127.0.0.1:9222. User opens http://localhost:6080/vnc.html (Cursor forwards :6080) to SEE+drive the browser; automation drives deterministic steps via Playwright connectOverCDP('http://127.0.0.1:9222'). Browser runs ON THE BOX so localhost:8180 (frontend) + localhost:8100 OAuth callback + outbound HTTPS to provider all resolve directly (no multi-port-forward). Driver: scripts/ralph/cdp_drive_login.mjs logs in (admin@zeroshield.io) + lands on ?tab=firewall-1-4 + registers a Linear streamable-http+oauth candidate ("Linear (manual OAuth)", url https://mcp.linear.app/mcp) → Pending card. WHY Linear: real OAuth 2.1 + DCR provider (GitHub MCP preset is bearer/PAT not oauth). Manual step: user clicks Authorize → control discovers Linear PRM/AS metadata → DCR → PKCE → Linear consent popup → user logs in w/ Linear creds → callback stores token → oauth_authorized flips → startControlOAuth poll auto-syncs → tools populate. VERIFY: poll GET /api/mcp-connector/servers/ for oauth_authorized=true + tools_count>0.
- PLAYWRIGHT ON THIS HOST (reusable for ALL UI items #14/#16/#18): stack IS running in Docker (frontend:8180, control:8100, gateway:8300 [401=up], broker:8311, all containers healthy). Login = admin@zeroshield.io / Adm1n!Pass#2024 (org=zeroshield). Playwright pkg at tests/e2e/node_modules BUT bundled chromium is UNSUPPORTED on ubuntu26.04 (`playwright install chromium/firefox` both fail). WORKAROUND: system chromium exists (/usr/bin/chromium-browser v149) → chromium.launch({executablePath:'/usr/bin/chromium-browser', headless:true, args:['--no-sandbox','--disable-dev-shm-usage','--disable-gpu']}). ESM `import "playwright"` ignores NODE_PATH → resolve via createRequire+abs path (path.resolve(__dirname,'../tests/e2e/node_modules/playwright')). SSRF guard requires the server URL to DNS-RESOLVE (non-resolving host → 400 at registration); use https://example.com/mcp (resolves, no OAuth metadata → discovery fails fast, no real-provider DCR). Selectors: login #email/#password + button /^sign in$/i (waits /api/auth/token/); panel BASE/?tab=firewall-1-4 wait "MCP Guardrails"; register modal btn /^Register Server$/i → dialog "Register MCP Server" → placeholder "my-mcp-server"(name)/"https://my-server.example.com/mcp"(url) + getByLabel("Transport")/getByLabel("Upstream authentication type") + btn /^Register$/ exact; Linear preset btn /Linear MCP/i. Card auth buttons match /^Authorize$|^Re-authorize$/i. Harness: scripts/playwright_mcp_b1_verify.mjs.
- B1 VERIFY RESULT (item#14): Linear stdio card = ONE gateway Authorize button (serverNeedsOAuth→startOAuth), "authorization required" badge, NO "no URL", NO control button; clicking opens popup (visibleError "OAuth start failed: unauthorized" = missing org gateway key in test session, NOT a B1 defect). HTTP oauth card = ONE Authorize (serverUsesHttpOAuth→startControlOAuth), pending badge, popup opens, click reaches DISCOVERY ("Could not discover OAuth metadata" for example.com) proving the #13 transport guard LETS HTTP THROUGH and blocks only stdio. The checklist's "NO OAuth button for stdio" = no BROKEN/control button; the single gateway button is the LEGIT authorize affordance (do not remove — Linear can't authorize otherwise).
- B1 FIX (item#13, root-cause, backend-only): the frontend + registration serializer were ALREADY correct (form filters oauth to HTTP transports MCPConnectorPanel.jsx:1483; serverUsesHttpOAuth guards transport+url; mutually-exclusive buttons; serializers.py:177 rejects oauth+non-HTTP at registration). The remaining defect was SERVER-SIDE in control MCPServerOAuthStartView (views.py): it gated on server.url but NOT transport → (a) "Server has no URL" 400 reachable for stdio, (b) it set auth_type="oauth" (:2582) bypassing the registration guard. FIX = added pure helper oauth_http_transport_error(server) enforcing transport∈(streamable-http,sse) BEFORE the url check + before any auth_type mutation; called at top of MCPServerOAuthStartView.post. Now stdio/websocket get a clear transport error (not "no URL"), can never reach discovery, and can never be flipped to oauth. "Server has no URL" only reachable for corrupted HTTP-row-without-url (serializer requires url for HTTP → unreachable via registration). *** RECONCILIATION (do NOT "re-fix"): the stdio+mcp-remote gateway Authorize button (serverNeedsOAuth→startOAuth→gateway oauth/start) is LEGITIMATE (Linear authorizes gateway-side, item#8/#10) — it is NOT a duplicate to delete; it's mutually exclusive with the HTTP-oauth control button so still exactly ONE button per card. *** TEST ENV NOTE: control has NO venv in-session; bootstrapped scratchpad venv (django6+DRF+channels+celery...); control migrations are Postgres-native (CREATE EXTENSION) → full manage.py test can't run on sqlite; view-guard tests are SimpleTestCase (no DB) → 6/6 green via pytest-django + standalone django.setup() script. Gate cmd: PYTHONPATH=ai_mesh_control:../shared DJANGO_SECRET_KEY=x DEBUG=True pytest ... -k OAuthStartViewTransportGuard.
- B1/B2 UX decision table (item#10, full in docs/mcp/oss-research-oauth-transport-ux.md): MCP has exactly 2 transports — stdio(subprocess/NO url/env-creds/NO oauth) + Streamable HTTP(single url endpoint/oauth applies); sse=deprecated HTTP variant. oauth is HTTP-only (RFC8707 resource=canonical HTTP URI, PRM/AS discovery is HTTP). RULES: oauth selectable ONLY for streamable-http|sse; BLOCK oauth on url-less stdio (that's B1's "block oauth+stdio") but stdio+mcp-remote is the LEGIT exception (row stays auth_type=none, gateway owns OAuth via serverNeedsOAuth→startOAuth). ≤1 Authorize button/card (gateway-cond vs http-oauth-cond are mutually exclusive). Fresh oauth server = PENDING badge, never a ready 0-tools card (B2); tools only after oauth_authorized+sync. B1 remaining defect = the CONTROL authorize path (views.py:2513) still reachable, emits 400 "Server has no URL" (:2526) + sets auth_type=oauth bypassing guard (:2582) → #13 must make it structurally unreachable (recommend: unify HTTP-oauth onto gateway oauth/start path). Industry norm (VS Code/Claude): "Needs Auth" status + ONE Authenticate btn that opens browser (guard the #42359 silent-no-op bug in #14).
- B3 ROOT CAUSE (item#12, PIN — de-risks #19-21): "MCP sandbox is temporarily unavailable" = mcp_sandbox_client._friendly_status for HTTP 502 (:57-58). Broker returns 502 for AGENT-CONNECTION errors during cold start (routes.py:185/191/196) but 503 only for docker-unavailable/sandbox-not-running (:63/163/166). Client _request_with_503_retry retries ONLY on 503 (:104) → a cold-start 502 is NOT retried → first tool call right after register (races npx-fetch+initialize) surfaces the hard error. FIX: (#19) eager warm on register/authorize/first-sync — call ensure + poll agent /health until proc-capable; honor the currently-IGNORED warm flag (routes.py:153). (#20) broker reports cold-start as 503 "provisioning"+Retry-After (retryable), NOT 502; add readiness poll+bounded backoff in mcp_sandbox_client treating 503-provisioning distinct from hard fail. (#21) verify first call succeeds or shows clear provisioning state. Client backoff ALREADY has jitter (mcp_sandbox_client.py:82). Concurrency model is mature: pool+reuse, request-id demux (_pending:76 + _reader_task:120, stdin serialized by proc.lock:372), caps 20/16/4. P8/P9 sizing: 15 MCPs (3org×5) ≤ global 20 & per-org 16 → no eviction; bottleneck = MAX_CONCURRENT_INITS=4 → PRE-WARM all 15 before concurrency test so steady-state skips init. No-503-storm needs: Retry-After on 429/503 + per-(org,server) circuit breaker (fast-fail+cooldown) + bounded client retries. Full: docs/mcp/oss-research-concurrency-load.md
- npm supply-chain (item#11, full in docs/mcp/oss-research-npm-untrusted-checklist.md): npx/uvx by default (a) fetches LATEST (unpinned=hijack window) + (b) runs postinstall lifecycle scripts w/ full privs BEFORE the MCP server starts (=Shai-Hulud 2025 RAT vector). But the server's OWN runtime code is legitimately run → Layer A (supply-chain) reduces what's fetched/run; Layer B (item#9 container) contains what runs. PATH ASYMMETRY (verified): gateway in-proc mcp_stdio_adapter.py HAS _PACKAGE_ALLOWLIST(:75) + _REQUIRE_PINNED_PACKAGES(:82,_is_pinned:154,enforced :362/:367) but BOTH DEFAULT OFF; broker/agent stdio_manager.py has NEITHER (only _ALLOWED_COMMANDS:37). NEITHER path sets npm_config_ignore_scripts (docker_manager.py:263 sets only NPM_CONFIG_CACHE/UV_CACHE_DIR) or NPM_CONFIG_REGISTRY. P7 #22-23 TODO: N1 npm_config_ignore_scripts=true (kills postinstall RCE), N2 enable REQUIRE_PINNED + add to broker path, N3 package-allowlist parity on broker path, N4 pre-bake vetted pkgs into image (offline-first), N5 registry-pin + egress-deny (dep-confusion), N6 uv/PyPI equivalents (wheels-only, sdist=setup.py runs), N7 no secrets in fetch env (already strong via _build_child_env). Acceptance = malicious-postinstall fixture proven inert via #30 egress capture.
- P7 hardening (item#9): _run_kwargs (docker_manager.py:247) is the only create-time knob. Cheap adds w/ ~0 compat risk: security_opt=['no-new-privileges:true'], cap_drop=['ALL'], ulimits=[Ulimit(nofile),Ulimit(nproc)], init=True, user='4000:4000'. memswap_limit=mem_limit closes swap-bypass. storage_opt={'size':..} ONLY on quota-capable FS (overlay2+xfs pquota) else it hard-errors create → gate on FS detect or reap-by-usage. runtime='runsc' (gVisor, hook already at :283) = biggest untrusted-code isolation win. EGRESS is the exfil channel: per-org bridge has full NAT (needed for npx) → needs host default-deny allowlist proxy (npm/PyPI + declared remote-MCP hosts only); exact-host match, no wildcards. Command allowlist (stdio_manager.py:37) ≠ package allowlist — broker path lacks _PACKAGE_ALLOWLIST that the in-proc path has (mcp_stdio_adapter.py:362). Details: docs/mcp/oss-research-docker-hardening.md

## P0 — Analysis
- [x] 1. Map gateway MCP request path (proxy→oauth→adapter→sandbox_client→broker) with file:line — docs/mcp/request-path-map.md (commit 4d970d1b)
- [x] 2. Map broker sandbox lifecycle (create→install→start stdio→health→reap) + resource limits — docs/mcp/broker-sandbox-lifecycle.md (commit fc268c58)
- [x] 3. Map control mcp_connector registration + oauth_authorized + tool-sync flow — docs/mcp/control-plane-flow.md (commit 8f952afd)
- [x] 4. Map frontend MCPConnectorPanel register/authorize/list/execute + the 4 bug sites — docs/mcp/frontend-panel-flow.md (commit 85a3c6e2)
- [x] 5. Write docs/mcp/ARCHITECTURE_AND_THREATS.md (isolation model + threat model + the 4 bugs) — docs/mcp/ARCHITECTURE_AND_THREATS.md (commit 0b2b17c2)

## P1 — OSS exploration (GitHub MCP)
- [x] 6. Study modelcontextprotocol/servers stdio server patterns (how official stdio servers start/handshake) — docs/mcp/oss-research-stdio-patterns.md (commit 0c5d877f)
- [x] 7. Study MCP auth spec + OAuth 2.1 (PKCE, RFC 9728 protected-resource + RFC 8414 AS metadata discovery) — docs/mcp/oss-research-oauth-spec.md (commit 52145d81)
- [x] 8. Study mcp-remote (how it wraps a remote HTTP MCP over stdio + does OAuth) — clarifies Linear's stdio+remote case — docs/mcp/oss-research-mcp-remote.md
- [x] 9. Study Docker sandboxing hardening (seccomp/gVisor/read-only rootfs/no-new-privileges/limits) + multi-tenant patterns — docs/mcp/oss-research-docker-hardening.md

## P2 — Deep research (web)
- [x] 10. OAuth-for-MCP + transport rules: why oauth needs an HTTP URL; correct UX for stdio-wrapped-remote — docs/mcp/oss-research-oauth-transport-ux.md (B1/B2 decision table)
- [x] 11. Docker multi-tenant isolation hardening checklist for running untrusted npm packages — docs/mcp/oss-research-npm-untrusted-checklist.md (npm supply-chain layer N1-N7 for P7 #22-23)
- [x] 12. Concurrency/load patterns for sandboxed subprocess MCP servers (pooling, warm start, backpressure) — docs/mcp/oss-research-concurrency-load.md (+ B3 ROOT CAUSE pinned)

## P3 — Fix B1 (oauth+stdio / 2 buttons / no URL)
- [x] 13. Form: OAuth selectable ONLY for HTTP transports; block oauth+stdio; render exactly one Authorize button (HTTP+oauth only); remove the dup/broken control authorize path — server-side guard added to MCPServerOAuthStartView (views.py oauth_http_transport_error); 6/6 view-guard tests pass (pytest-django, SimpleTestCase). Frontend already correct (no change). Playwright = #14.
- [x] 14. Playwright verify: register Linear(stdio) → NO OAuth button, no "no URL" error; register an HTTP oauth server → exactly one Authorize, popup opens — scripts/playwright_mcp_b1_verify.mjs, 8/8 asserts PASS ×2 (system chromium). Linear stdio=1 LEGIT gateway button (not the broken control one) + no "no URL"; HTTP oauth=exactly 1 Authorize + popup opens + pending badge + reaches discovery (guard lets HTTP through).

## P4 — Fix B2 (lists 0-tools before oauth)
- [x] 15. HTTP oauth server on register shows a distinct "Pending authorization" state (or triggers OAuth inline); NOT a normal 0-tools card; tools appear only after oauth_authorized + sync — renderServerCard now shows amber "Pending authorization" badge (replaces grey "Unknown") + "Authorize to load tools" (replaces "0 tools") when awaitingAuth; sync stays disabled. build✓ + 26/26 unit✓; scripts/playwright_mcp_b2_verify.mjs 6/6 PASS + screenshot; B1 re-verify 8/8 (no regression).
- [ ] 16. Playwright verify: register → pending state → authorize → tools populate

## P5 — Fix B4 (modal focus loss)
- [x] 17. Stabilize the Add Server modal/form subtree (define fields outside render / stable keys / no portal-children recreation) so focus persists per keystroke — NO CODE CHANGE NEEDED (prior fix holds): Dialog.jsx onCloseRef(:22)+stable handleKey useCallback + effect deps [open,handleKey](:68) → focus-trap effect runs only on open-toggle not per keystroke; MCPConnectorPanel has NO component defined in render (only top-level fn decls StatusDot/StatCard/Inner/Panel), render helpers are function CALLS {renderServers()}. VERIFIED behaviorally: scripts/playwright_mcp_p1_dialog_focus_repro.mjs (system chromium) → b4BugConfirmed=false, focusLossFields=[], focus kept 24 keystrokes across name/url/description/stdio-command/bearer. (Parallel loop P1.3 = same result.)
- [x] 18. Playwright verify: type a long string into each field without losing focus; presets prefill correctly — FOCUS: p1_dialog_focus_repro 24 keys×5 fields focus kept (item#17) + b4_preset_verify 37 keys on prefilled field focus kept. PRESET PREFILL: GitHub MCP preset (requiresAuth) opens modal prefilled name="GitHub MCP"/url="https://api.githubcopilot.com/mcp/"/transport="streamable-http"/auth_type="bearer". scripts/playwright_mcp_b4_preset_verify.mjs 5/5 PASS + screenshot.

## P6 — Fix B3 (sandbox temporarily unavailable)
- [ ] 19. Eager sandbox provisioning on register/authorize/first-sync (warm the per-org sandbox) 
- [ ] 20. Readiness poll + bounded backoff retry in mcp_sandbox_client; broker returns clear "provisioning" not just 503
- [ ] 21. Verify: first tool call after register succeeds or shows a clear provisioning state (no hard "temporarily unavailable")

## P7 — Sandbox isolation hardening (the dev-plan)
- [ ] 22. Enforce resource limits per sandbox (CPU, memory, disk, timeout) + verify a runaway MCP is contained
- [ ] 23. Confirm NO unknown npm package executes on the host — only inside the per-org sandbox
- [ ] 24. Reaper/idle cleanup correctness (no orphan sandboxes; restart-safe registry) 
- [ ] 25. Per-org credential/env isolation (Org A env/secrets never visible in Org B sandbox)

## P8 — 15-MCP parallel harness
- [ ] 26. Provision 3 orgs × 5 servers (§1.4); extend scripts/mcp_live_matrix_harness.py / mcp_pipeline_matrix_live.py
- [ ] 27. Harness drives parallel tool calls across all 15 via the gateway (OpenAI/MCP path), capturing egress + audit per call

## P9 — Concurrency / load / leakage
- [ ] 28. Concurrency: fire parallel tool calls across all 15 (Everything.echo/add deterministic) — assert correct isolation + no dropped/mixed responses
- [ ] 29. Load: sustained calls — sandbox reuse/pooling holds, no exhaustion, no 503 storms, resource limits respected
- [ ] 30. Cross-tenant leakage: Org A cannot list/call Org B's servers, cannot read B's tool results/creds; plant a canary in Org B (Everything/Filesystem) and prove Org A never observes it on any channel
- [ ] 31. OAuth/transport correctness across the fleet under load (no stdio server ever attempts OAuth; HTTP oauth servers authorize cleanly)

## P10 — Recursive verification
- [ ] 32. Re-run P3–P9 end-to-end (in-process + live); adversarial pass; all green 3× → emit COMPLETE